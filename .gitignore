node_modules/
*.log
.env
.DS_Store

# Claude Code
CLAUDE.md
.claude/
.claudeignore

# Build output — built by Vite during Docker build
web/dist/

# MediaPipe models — downloaded during Docker build
web/public/models/

# MediaPipe WASM in public/ — duplicated from web/mediapipe/, copied at build time
web/public/mediapipe/

# Private notes and planning artifacts — repo is PUBLIC, keep these local
PRIVATE-*.md
plan.md
planning/
mockups/
*.private.md

# Python venvs (eval data-pull tooling)
.venv/
eval/.venv/
